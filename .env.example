# Database connection string (PostgreSQL)
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5432/app_db"

# Note: Google Gemini API keys can be configured directly inside the application UI
# on the /settings page with multi-key rotation and real-time quota tracking.
