# Default verification and attempt diagnostics

User authorization: enable selective checking by default, prefer the player's Jev key, use a server administrator key when absent, skip Jev when neither key exists. Do not replace unavailable Jev with Gemini. Existing uncertainty review is distinct from service unavailability. User confirmed service outage/quota/timeout must also continue unchecked, with a diagnostic marker.

1. Test and implement pure credential resolution with explicit key source and separate requested/effective enablement. Server-only administrator environment variables; never expose administrator key suffixes. Preserve personal opt-out and provider/key boundaries.
2. Enable new settings by default in the not-yet-deployed feature migration and schema. Update owner-facing status and environment documentation. Existing shared Gemini credentials remain forbidden.
3. Add a per-attempt diagnostics table and bounded versioned payload, with session ownership inherited and cascade deletion. Collect successful, skipped and blocked decisions in memory; persist the snapshot and final outcome only after the response. User explicitly requires logging not to delay gameplay. Capture selected questions, source evidence, immutable state, draft/repair, model decisions, timings and available usage without credentials. Failed diagnostics writes must not change game results.
4. Provide an owner-only paginated diagnostic read endpoint. Keep operational telemetry separate from evidence used to establish campaign facts. Bound retention and sizes; expose truncation and safe error codes.
5. Verify no-key bypass, administrator fallback, personal precedence/opt-out, no secret leakage, failed-attempt retention, cross-owner access, existing guard mechanics and full repository checks. No commit/push/deployment requested in this increment.


Implemented credential resolution/defaults, explicit outage bypass with no Gemini substitution, no admission of unverified independent gains/agreement revisions, attempt diagnostics and owner-only paginated endpoint. Independent review found truncation-marker loss and potentially blocking diagnostic SQL: fixed cumulative markers and bounded database operations, then moved all persistence into Next.js after in response to the user’s latency requirement. Added an actual orchestrator regression proving committed response exists before any diagnostic row is written. Full final checks pending.

Final verification: 274 tests passed; lint and production build passed. A TypeScript inference error in the newly expanded unavailable-provider test was corrected with an explicit return type; the subsequent production build passed. Independent review of the deferred implementation found no remaining blocker. The user-requested hierarchy and outage behavior are implemented, and actual turn integration proves diagnostics do not start persistence before the committed response returns. Only Next.js after performs diagnostic SQL/cleanup. No production keys were changed and no commit/push/deploy was performed. Configure NARRATIVE_ADMIN_OPENROUTER_API_KEY (or the TypeSafe alternative) on the server to activate administrator fallback.
