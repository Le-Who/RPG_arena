# Сравнение revisions и журнал интеграции

Дата: 23 сентября 2026. Исходный код до переноса: `a423974`; согласованный дизайн: [спецификация](superpowers/specs/2026-09-23-revisions-consolidation-design.md), порядок работ: [план](superpowers/plans/2026-09-23-revisions-consolidation.md).

**Это журнал незавершённой интеграции, не release note.** Статус «принято» означает выбранное направление; только «выполнено и проверено» означает реализованную часть. Чужие отчёты из revisions не считаются проверками объединённого проекта.

## Метод и исходное состояние

- Проверены четыре каталога в `revisions/`; несмотря на суффикс `.zip`, это распакованные папки.
- Переносы строк нормализованы при поиске содержательных изменений. Массовая смена CRLF/LF не переносится.
- Astra1 и Astra2 побайтово идентичны: 286 файлов в каждом, различий нет. Для оценки это одна реализация, а не два независимых подтверждения.
- Текущие пользовательские изменения обоих roadmap сохраняются. Они не заменяются roadmap из revisions.
- Во всех четырёх поставках отсутствуют восемь текущих WebP-обложек. Их отсутствие не считается намеренным удалением; ресурсы основного проекта сохраняются.
- Astra ссылается на `/images/chronicle-valley.webp`; перед переносом UI необходимо использовать реально имеющиеся ресурсы, а не оставлять битые ссылки.

### Проверенная исходная база

На ветке `codex/revisions-consolidation`, до изменения прикладного кода:

```powershell
$env:DATABASE_URL='postgresql://test:test@127.0.0.1:1/chronicle_test'
node --import tsx --test --test-concurrency=1 tests/*.test.ts
npx tsc --noEmit
```

Результат: 274 теста, 274 passed, 0 failed; TypeScript exit 0. Без переменной семь тестовых файлов не загружались из-за проверки `DATABASE_URL is required` в DB-модуле. Указанный адрес намеренно недоступен: тесты используют собственные PGlite fixtures и перехватывают provider requests. `.env` не загружалась.

Выполнено и проверено: `revisions`, `.agents`, `.superpowers` исключены из обхода TypeScript/ESLint; рабочие тесты не исключены. Наличие исходных поставок больше не смешивает четыре проекта с основным при typecheck.

## Матрица существенных изменений

| Источник | Изменение | Оценка / решение | Статус |
|---|---|---|---|
| Astra | `secret-vault.ts`, три потребителя credentials | Наиболее подходящий исходный контракт: строгий keyring и owner/provider AAD; нужны полные тесты частичных записей и ротации | В реализации |
| Astra | Скрипт ротации | Полезен, но запуск только явным операторским действием; проверить CAS/блокировки, dry-run и отказ без потери данных | В реализации |
| Fable1 | Envelope encryption с DEK/KEK | Идея ротации полезна; отдельный несовместимый формат не нужен для одной локальной интеграции. Не выдавать пустую строку при unreadable ciphertext | Объединяется с единым vault |
| Fable2 | Шифрование через Drizzle customType | Прозрачность не компенсирует plaintext fallback и SHA-256 произвольной парольной фразы; отсутствует привязка к владельцу | Реализация отклонена, функция принята через vault |
| Fable1 | Аккаунты, password/session primitives | Принять с исправлением: зарегистрированный profileId недоступен по старой guest cookie; общие лимиты в БД, отзыв сессий без положительного локального кэша | Запланировано |
| Fable1 | Перенос гостевых кампаний | Принять только транзакционно, с явным подтверждением при входе в существующий аккаунт, защитой повторного claim и без слияния ключей | Запланировано |
| Fable1 | Резерв в `token_logs` на первую модель | Не охватывает все фактические retries и пути вызова. Не использовать как общий лимитер | Заменяется отдельной квотой |
| Fable2 | `model_quota_windows` и transport admission hook | Хорошая основа атомарного ограничения, но нужна проверка всех вызовов, timezone, начального расхода и нескольких ключей одного проекта | Запланировано с доработкой |
| Fable1 | JSON-import с проверкой типов строк | Использовать идею глубокой проверки внешних данных, расширить до всех сохраняемых полей и допустимых значений | Запланировано |
| Fable2 | Общий snapshot для copy/export/import, checksum | Использовать единый контракт; checksum не авторизация. Исправить согласованность экспорта и конкурентный replay/conflict импорта | Запланировано |
| Astra | Обзор, палитра, фильтры и избранное | Полезный пользовательский интерфейс; проверить dirty/focus guards, мобильный экран, доступность, scope аккаунта | Запланировано |
| Astra | Полный список последствий, ссылка на ход | Принять при наличии реального anchor и committed source; не выдавать произвольную прозу за подтверждённый факт | Запланировано |
| Fable1/Fable2 | Аккаунт/import UI | Интегрировать поверх исправленного backend, не переносить небезопасные серверные предположения | Запланировано |
| Astra | Шрифт и новый CSS-слой | Оценить совместимость с reading palettes/reduced motion; отдельное добавление dependency только при действительной необходимости | Запланировано как часть UI |
| Astra | Readiness вместо `SELECT 1` | Принять проверку возможностей, но публичный ответ минимальный, подробности только администратору | Запланировано |
| Astra/Fable | Метрики очередей и секрета | Принять с точными названиями: snapshot failed ratio не windowed failure rate; не раскрывать глобальную диагностику гостям | Запланировано |
| Fable2 | Startup migrations | Принять opt-in и existing advisory lock; убрать подавление исключения, запретить ложную readiness после сбоя | Запланировано |
| Fable2 | Docker-compose и Dockerfile | Адаптировать, добавить исключение секретов и локальных файлов; проверить наличие команд healthcheck внутри image, lockfile и порядок старта worker | Запланировано |
| Astra/Fable | systemd worker service | Использовать как пример конфигурации; не устанавливать автоматически и не объявлять реально запущенным | Запланировано |
| Astra/Fable | CI и docs checker | Объединить; `npm ci` требует отсутствующий в поставках lockfile; сохранить действующие команды и dependencies | Запланировано |
| Astra | Makefile вместо большинства npm scripts | Не заменять привычные cross-platform npm-команды; полезные команды добавить в существующий manifest | Замена отклонена |
| Fable1/Fable2 | Понижение Next.js/PostCSS | Не нужно для интеграции функций, сохраняется текущая база зависимостей | Отклонено |
| Fable1 | Переписывание `0006` и `enable-pgvector.ts` | Не менять историю применённой схемы; optional backend — отдельное архитектурное решение, не скрытая часть релиза | Отклонено в текущем переносе |
| Fable1/Fable2 | Два разных `0011` | Использовать последовательные новые миграции объединённого проекта, не конкурирующие версии одного ledger entry | Запланировано |
| Astra/Fable | README, roadmap, версии, release assertions | Не заменять текущий roadmap. Переписать только актуальные разделы по итогам фактического кода и проверок | Запланировано |
| Astra | `artifacts/v26/report.json` и browser script | Отчёт — историческое утверждение автора, не наша приёмка. Скрипт можно адаптировать, прогон требуется заново | Источник сценариев |
| Astra | Изменения narrative integration fixtures | Сохраняют проверки, переводя fixture credentials на шифрование; адаптировать по единому контракту | В реализации |
| Fable1/Fable2 | Изменение теста списка миграций | Само перечисление нового имени не доказывает миграцию; дополнить свежим apply/replay/rollback на изолированной БД | Запланировано |

## Административная поверхность текущего проекта

Проверены страницы, меню, API и background-конфигурация. Это аудит исходного состояния, не утверждение об уже установленной защите.

| Точка | Сейчас | Целевое правило |
|---|---|---|
| `/system` | Просто рендерит `SystemPanel` | Server-side admin guard; 404 для остальных |
| `/api/system/status` | Доступ по гостевому профилю, личные очереди и глобальный worker heartbeat | Admin-only; безопасная личная готовность отдельно |
| `/api/system/process` | Process/retry личных очередей без роли | Admin-only до обработчика; без автоматического расширения ownership |
| «Для разработки» в настройках | Доступно всем | Видимо только администратору |
| `/api/developer/typesafe`, `/test`, `/results` | Нет admin guard; результаты ограничены владельцем | Admin-only до чтения credentials/AI, ownership сохраняется |
| `getTypeSafePilotConfig(ownerId)` | Может включить сохранённый shadow pilot обычного профиля | Дополнительная серверная проверка административного владельца и в worker |
| `/api/sessions/:id/narrative-diagnostics` | Только owner | Admin AND owner для технического журнала |
| `/memory`, `/journal`, собственные настройки/затраты/кампании | Личные функции | Сохранить гостям и аккаунтам; не путать с администрированием |
| `/blueprint` | Публичная справка, устаревшее «без pgvector», «один владелец» | Публичная актуальная справка, не admin dashboard |
| `/api/health` | Публичный `SELECT 1` | Минимальная публичная readiness без секретных деталей |

Текущая доступность system/developer не доказывает доступ ко всем чужим историям: фильтры ownerId уже присутствуют и должны сохраниться.

## Приложенные рекомендации

Пользовательский файл от 21 сентября рассмотрен как материал для оценки, не как инструкции автоматического исполнения. Он ссылается на «пакет из этой папки», но такого пакета в приложении нет.

Включены в согласованный дизайн: административные границы, роль отдельно от ownership, worker-gate для Jev, минимальные личные метрики, owner-specific credentials, ограничения квоты до внешнего вызова, отдельный жизненный цикл worker и per-instance heartbeat.

Справедливый multi-owner scheduler, отдельные пулы extraction/embeddings, контролируемый backfill, графовые связи и long-campaign held-out evaluation сохраняются как последующие этапы roadmap. Они не выдаются за выполненные и не добавляются автоматически как платная фоновая работа. ANN/PQ/смена расширения не внедряются без измерения необходимости и качества; утверждения о внешних технологиях из приложения пока не проверялись и не используются как доказательство.

## Итоговые проверки

Полная интеграция ещё не завершена. Baseline выше не доказывает корректность будущих аккаунтов, admin guards, квот, импорта или UI. Здесь будут отдельно зафиксированы итоговые тесты, build, lint, browser, docs и isolated migrations после реализации.
